# ai_chatbot
AI-Powered Customer Support Chatbot built on Retrieval-Augmented Generation (RAG). Uses Sentence-BERT for vector search against a custom Knowledge Base (KB) to provide accurate, context-grounded answers via a FastAPI backend and Docker deployment.
